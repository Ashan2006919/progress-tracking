export default function WorkoutRoutinePage() {
    return (
      <>
        <h1 className="text-2xl font-bold mb-4">Workout Routine</h1>
        {/* Add form elements here later: workoutType, duration, etc. */}
      </>
    );
  }
